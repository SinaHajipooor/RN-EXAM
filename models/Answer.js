class Answer {
    constructor(id, text, questionId, type, file) {
        this.id = id;
        this.text = text;
        this.questionId = questionId;
        this.type = type; // drost ya qalat budne soal
        this.file = file;
    }
}

export default Answer;
