class Question {
    constructor(id, text, lessonId, file, hardshipLevel) {
        this.id = id;
        this.text = text;
        this.lessonId = lessonId;
        this.file = file;
        this.hardshipLevel = hardshipLevel;
    }
}

export default Question;
