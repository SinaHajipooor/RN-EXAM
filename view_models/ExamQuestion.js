class ExamQuestion {
    constructor(id, questionText, questionFile, examId, questionId) {
        this.id = id;
        this.questionText = questionText;
        this.questionFile = questionFile;
        this.examId = examId;
        this.questionId = questionId;
    }
}

export default ExamQuestion;
